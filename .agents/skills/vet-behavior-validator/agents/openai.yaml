interface:
  display_name: "Vet Behavior Validator"
  short_description: "Black-box Vet validation."
  default_prompt: "Use $vet-behavior-validator to validate this workflow against a source-blind behavior contract."

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "value",
    "priority",
    "risk",
    "alignment",
    "implementationScope",
    "proofNeeded",
    "automationDecision",
    "humanQuestion"
  ],
  "properties": {
    "value": { "type": "string", "enum": ["low", "medium", "high"] },
    "priority": { "type": "string", "enum": ["low", "medium", "high"] },
    "risk": { "type": "string", "enum": ["low", "medium", "high"] },
    "alignment": { "type": "string", "enum": ["yes", "no", "unclear"] },
    "implementationScope": { "type": "string" },
    "proofNeeded": { "type": "string", "enum": ["none", "CI", "UI", "GIF", "service"] },
    "automationDecision": {
      "type": "string",
      "enum": ["implement", "manual-review", "blocked", "reject"]
    },
    "humanQuestion": { "type": "string" }
  }
}

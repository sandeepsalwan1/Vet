{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "bugsFound",
    "fixesMade",
    "checksRun",
    "remainingRisk",
    "proofNeeded",
    "mergeRecommendation",
    "humanQuestion",
    "unifiedDiff"
  ],
  "properties": {
    "bugsFound": {
      "type": "array",
      "items": { "type": "string" }
    },
    "fixesMade": {
      "type": "array",
      "items": { "type": "string" }
    },
    "checksRun": {
      "type": "array",
      "items": { "type": "string" }
    },
    "remainingRisk": { "type": "string", "enum": ["low", "medium", "high"] },
    "proofNeeded": { "type": "string", "enum": ["none", "CI", "UI", "GIF", "service"] },
    "mergeRecommendation": {
      "type": "string",
      "enum": ["ready", "ready-human-review", "blocked"]
    },
    "humanQuestion": { "type": "string" },
    "unifiedDiff": {
      "type": "string",
      "description": "Unified git diff for any safe fixes, or an empty string when no fixes are made."
    }
  }
}
